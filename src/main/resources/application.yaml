spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scripts?useLegacyDatetimeCode=false&serverTimezone=UTC
    password: pass
    username: scripts_master
  jpa:
    #show-sql: true
    properties:
      hibernate:
        #format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
server:
  error:
    include-message: always